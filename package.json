{
  "name": "research-code",
  "private": true,
  "description": "Python research project — dependency install mirrors npm-style one-liner via npm scripts",
  "scripts": {
    "install-deps": "python3 -m pip install -r requirements.txt"
  }
}
